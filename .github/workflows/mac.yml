name: mac.yml
on: push

jobs:
  build:
    name: Build on Windows
    runs-on: windows-2022
    steps:
      - name: Check path
        run: vswhere.exe -latest -requires Microsoft.VisualStudio.Component.VC.Llvm.Clang -property installationPath
