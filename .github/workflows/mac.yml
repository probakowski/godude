name: mac.yml
on: push

jobs:
  build:
    name: Build on Mac OS
    runs-on: macos-15-xlarge
    steps:
      - name: Check path
        run: bash -c 'ls -la /usr/bin/llvm-gcc; llvm-gcc --version; find ${PATH//:/\/ } -name llvm-*; true'
